#include <stdio.h>#include <stdlib.h>#define EXIT_FAILURE 1#define ARG_MIN 1int main( int argc, char *argv[] ) {	FILE *fp;	char recipe_data[128];	int recipe_id = 1;	int recipe_selected = -1;	if( argc < ARG_MIN + 1 ) {		printf("指定する引数の数が違います.\n");		exit( EXIT_FAILURE );	}		fp = fopen( argv[1], "r" );	if( fp == NULL ) {		printf("FILE OPEN ERROR.\n");		exit( EXIT_FAILURE );	}	if( argc == ARG_MIN + 2) {		recipe_selected = atoi(argv[2]);	}	while( fscanf( fp, "%s", recipe_data ) != EOF ) {		if( recipe_selected == -1 || recipe_selected == recipe_id){			printf("%d: %s\n", recipe_id, recipe_data );		}		recipe_id++;	}		fclose( fp );	return 0;}